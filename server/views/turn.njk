{% extends  layout %}
{% block  content %}
    {%- from 'card.njk' import card -%}
  <h1>{{message}}</h1>
  <p>It's {{player}}'s turn!</p>
  {% if showCards == true %}
    <p>Table card: {{tableCard}}</p>
    <p>Here are your cards:</p>
      {% for mycard in cards %}
        {% set newCardUrl = [cardUrl, loop.index0] | join %}
        <div>{{ card({url: newCardUrl, value: mycard.value, color: mycard.color}) }}</div>
      {% endfor %}
    </br>
    <a href={{url}}>Take Card</a> 
  {% else %}
    <p>Click to see your cards!</p>
    <a href={{url}}>{{text}}</a>
  {% endif %}
{% endblock %}